syntax = "proto3";

service Greeter {
    rpc Hi (Request) returns (Response) {};
}

service Learning {
    rpc Hi (Request) returns (Response) {};
}

message Request {
    string name = 1;
}

message Response {
    string msg = 1;
}